<!-- 
	pluralize is a ruby method that returns the plural form of the word in the string.
	if the fist parameter, count is specified, the singular from of the string will be returned if count ==1.
	For any other value of count, the plural form of the string will be returned.
	count is a ruby method that returns the number of elements
-->
<h1>View Actor's <%= pluralize(@movies.count, "Movie") %></h1>
<table>
	<thead>
		<th>Title</th>
		<th>Duration</th>
		<th>Rating</th>
		<th>Release Date</th>
	</thead>
	<tbody>
		<!--
			each is a ruby method that iterares over a code block for each element in the array of objects
		-->
		<% @movies.each do |movie| %>
		<tr>
			<td><%= movie.title %></td>
			<td><%= movie.duration %></td>
			<td><%= movie.rating %></td>
			<td><%= movie.release_date.strftime("%Y-%m-%d") %></td>
		</tr>
		<% end %>
	</tbody>
</table>
<a href="/actors">View Actors</a>